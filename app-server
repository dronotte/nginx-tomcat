yum update -y && yum upgrade -y && \
yum install -y tomcat wget

Скачать варник дженкинса
wget https://get.jenkins.io/war-stable/2.277.4/jenkins.war

Положить его в apache-tomcat
cp jenkins.war /usr/share/tomcat/webapps/jenkins.war

systemctl stop firewalld && systemctl disable firewalld


выключим селинух
setenforce 0
sed -i 's/^SELINUX=.*/SELINUX=disabled/g' /etc/selinux/config


Закинем конфигурационные файлы томката (ipv6 работал по умолчанию, а нужно ipv4)

src: /etc/ansible/roles/tomcat/templates/tomcat.conf
dst: /usr/share/tomcat/conf/tomcat.conf

src: /etc/ansible/roles/tomcat/templates/server.xml
dst: /usr/share/tomcat/conf/server.xml


Запустим tomcat

systemctl enable tomcat
systemctl start tomcat
